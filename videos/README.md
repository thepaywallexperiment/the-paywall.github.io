# Current video

Do not commit large video files here.

Set `currentVideoUrl` in `state.json` to a CDN or hosting URL, for example:

```json
"currentVideoUrl": "https://cdn.example.com/current.mp4"
```

Or place a file at `videos/current.mp4` for local testing.
